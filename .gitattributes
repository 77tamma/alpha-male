# Normalize line endings so the repo is identical regardless of who clones it.
* text=auto eol=lf

# Binary assets must never be touched.
*.mp4  binary
*.jpg  binary
*.jpeg binary
*.png  binary
*.webp binary
*.zip  binary
